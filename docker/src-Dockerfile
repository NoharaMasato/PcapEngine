FROM ubuntu:latest

WORKDIR /
RUN apt-get update -y && apt-get install -y netcat iproute2 iputils-ping tcpdump
RUN ip route add 192.168.1.0/24 via 192.168.0.3

# CMD ["nc","pkt-dst","8080"]
CMD ["nc","-l","8080"]
